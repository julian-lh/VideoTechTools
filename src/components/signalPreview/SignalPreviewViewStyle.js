import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

    canvas: {
        zIndex: 0,
        flex: 1,
        minWidth: 20,
        minHeight: 20,
        backgroundColor: '#eee'
    },

});

export {styles};